--instalar
Ubuntu 24.04.1 LTS  
Docker Desktop     
Redis Insight         
--contenedor y imagenes docker           
docker run -p 3000:80  verdefast          
docker run --name vf -d --link verdefast:redis -p 8081:8081 -e REDIS_HOSTS=local:redis rediscommander/redis-commander:latest            
--ubuntu                        
sudo apt install php php-                
sudo apt install php                
sudo apt update                    
cd /mnt/c                   
--servidor                       
php -S localhost:9500                     
./ngrok http 9500                         
